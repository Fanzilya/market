# Админ
 1) админ создаёт компонент
 2) компонент берётся и добавляется в схему
 3) выбирается объязательный или нет
 4) выбирается нужно ли добавлять ему текст и поле
 5) ставиться поле
 6) добавляется текс


# Пользователь
 1) выбирается чек бокс
 2) вводиться значение